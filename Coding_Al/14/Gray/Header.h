#pragma once
#include <iostream>
#include <cmath>
#include <vector>
using namespace std;
// Сигнатуры функций
unsigned bin_to_gray(unsigned num);
unsigned gray_to_bin(unsigned num);
string to_binary(unsigned num);
// Сигнатуры тестов
void tests();